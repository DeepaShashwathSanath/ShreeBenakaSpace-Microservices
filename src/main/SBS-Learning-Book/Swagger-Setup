I gave:
http://localhost:8081/swagger-ui/index.html
Whitelabel Error Page
This application has no explicit mapping for /error, so you are seeing this as a fallback.

Fri Apr 03 00:52:43 IST 2026
There was an unexpected error (type=Not Found, status=404).

Ahh — this one is actually expected, not an error in your code 👍

🧠 What’s happening

You opened:

http://localhost:8081/swagger-ui/index.html

👉 But your project does NOT have Swagger configured yet

So Spring Boot is saying:

404 Not Found

And showing:
👉 Whitelabel Error Page

✅ Your service is probably working fine

Swagger ≠ default feature

👉 It only works if you add Swagger dependency

🔍 First, verify your API is working

Try this in browser or Postman:

👉 GET
http://localhost:8081/users

If you see:

[]

👉 ✅ Your service is working perfectly

🚀 Now let’s ADD Swagger (very useful)
🔹 Step 1: Add dependency in pom.xml
<dependency>
    <groupId>org.springdoc</groupId>
    <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
    <version>2.5.0</version>
</dependency>
🔹 Step 2: Reload Maven

👉 Click:

Reload Project 🔄
🔹 Step 3: Restart application
🔹 Step 4: Open Swagger

Now try:

http://localhost:8081/swagger-ui.html

OR

http://localhost:8081/swagger-ui/index.html
🎯 Expected result

You’ll see:
👉 Swagger UI with:

GET /users
POST /users
💡 Why Swagger is important
Easy API testing
No need Postman initially
Used in real projects
💬 Small reassurance

You didn’t do anything wrong.

This confusion happens because:
👉 In monolith, maybe Swagger was already added
👉 In microservice, you started fresh

🚀 Your next step
Check /users API
Add Swagger
Open Swagger UI

Then tell me:

👉 “Swagger working”

Next we’ll:
🔥 Connect your user-service to database (real backend level)
